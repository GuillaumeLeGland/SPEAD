close all
clear all

ESD = 5.0;
RAD = ESD/2;

%------------------------------------
% VOL = (4/3)*pi*RAD^3; OK.
% VOL = (1/6)*pi*ESD; %ESTA MAL!!!!
%------------------------------------
% ESD = ((3*VOL)/(4*pi))^(1/3) * 2;
% ESD = ((6/pi)*VOL)^(1/3);
%------------------------------------
% RADIUS = ((3*VOL) / (4*pi))^(1/3);
%------------------------------------
